[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "barelysmash-atlas-metrics"
version = "0.2.0-alpha.1"
description = "Metric engine for Atlas."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]